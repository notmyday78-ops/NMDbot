const { createCanvas, registerFont } = require('canvas'); const fs = require('fs'); registerFont('C:/Windows/Fonts/segoeui.ttf', { family: 'PegasusFont' }); const canvas = createCanvas(200, 200); const ctx = canvas.getContext('2d'); ctx.font = 'bold 36px \'PegasusFont\', sans-serif'; ctx.fillText('Hello', 10, 50); fs.writeFileSync('test.png', canvas.toBuffer()); console.log('Done');
